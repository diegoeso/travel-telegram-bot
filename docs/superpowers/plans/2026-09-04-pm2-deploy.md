# PM2 Deploy Script Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Actualizar `deploy.sh`, `ecosystem.config.cjs` y `DEPLOY.md` para que el despliegue con PM2 compile e inicie el bot de forma fiable en el VPS.

**Architecture:** Script bash idempotente en el root del repo: valida Node/.env, instala con `npm ci` (incluye devDeps), genera Prisma, compila TypeScript, hace `npm prune --omit=dev`, y arranca o reinicia PM2 con `--env production`. Sin `git pull` dentro del script.

**Tech Stack:** Bash, Node.js ≥ 22, npm, Prisma, TypeScript (`tsc`), PM2, Redis del host.

## Global Constraints

- Runtime de despliegue: PM2 en el host (no Docker).
- Sin `git pull` en `deploy.sh`; el operador corre `git pull origin main && bash deploy.sh`.
- Dependencias: `npm ci` → `prisma generate` → `npm run build` → `npm prune --omit=dev`.
- Redis: `127.0.0.1:6379` (sistema); el script no lo instala.
- Bot mode: `polling`; no tocar nginx ni webhook.
- No modificar secretos del `.env`.
- Commits solo si el usuario lo pide explícitamente en la sesión de ejecución (omitir pasos de commit si no hay permiso).
- Responder al usuario en español.

## File Structure

| Archivo | Responsabilidad |
|---------|-----------------|
| `deploy.sh` | Orquestación del deploy (validación, build, prune, PM2, verificación online) |
| `ecosystem.config.cjs` | Definición del proceso PM2 (`cwd`, env production, logs) |
| `DEPLOY.md` | Documentación alineada con el flujo real del script |

---

### Task 1: Actualizar `ecosystem.config.cjs`

**Files:**
- Modify: `ecosystem.config.cjs`

**Interfaces:**
- Consumes: PM2 lee este archivo en `pm2 start|restart ecosystem.config.cjs --env production`
- Produces: app name `travel-telegram-bot`, `script: dist/index.js`, `cwd: __dirname`, `env_production.NODE_ENV=production`

- [ ] **Step 1: Reescribir `ecosystem.config.cjs`**

Reemplazar el contenido completo por:

```js
module.exports = {
  apps: [
    {
      name: "travel-telegram-bot",
      cwd: __dirname,
      script: "dist/index.js",
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: "256M",
      env_production: {
        NODE_ENV: "production",
        LOG_LEVEL: "warn",
      },
      error_file: "./logs/error.log",
      out_file: "./logs/out.log",
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      merge_logs: true,
    },
  ],
};
```

- [ ] **Step 2: Verificar que Node carga el módulo**

Run:

```bash
cd /home/goadmin-bot/htdocs/bot.goadmintravel.com
node -e "const e=require('./ecosystem.config.cjs'); const a=e.apps[0]; if(a.name!=='travel-telegram-bot') process.exit(1); if(a.cwd!==__dirname) process.exit(2); if(!a.env_production||a.env_production.NODE_ENV!=='production') process.exit(3); console.log('ecosystem OK', a.cwd);"
```

Expected: `ecosystem OK` seguido de la ruta absoluta del proyecto.

- [ ] **Step 3: Commit (solo si el usuario lo pidió)**

```bash
git add ecosystem.config.cjs
git commit -m "$(cat <<'EOF'
fix: set PM2 cwd for telegram bot process

EOF
)"
```

---

### Task 2: Reescribir `deploy.sh`

**Files:**
- Modify: `deploy.sh`

**Interfaces:**
- Consumes: `.env` con `TELEGRAM_BOT_TOKEN` y `DATABASE_URL`; `ecosystem.config.cjs` de Task 1; Node ≥ 22; `pm2` en PATH
- Produces: `dist/` compilado; proceso PM2 `travel-telegram-bot` en estado `online`; exit code 0 en éxito

- [ ] **Step 1: Escribir el script completo**

Reemplazar `deploy.sh` por:

```bash
#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT_DIR"

APP_NAME="travel-telegram-bot"
REQUIRED_NODE_MAJOR=22

echo "=== Travel Telegram Bot - Deploy ==="
echo ">> Directorio: $ROOT_DIR"

require_cmd() {
  if ! command -v "$1" >/dev/null 2>&1; then
    echo "ERROR: comando requerido no encontrado: $1" >&2
    exit 1
  fi
}

require_cmd node
require_cmd npm
require_cmd npx
require_cmd pm2

NODE_MAJOR="$(node -p "process.versions.node.split('.')[0]")"
if [[ "$NODE_MAJOR" -lt "$REQUIRED_NODE_MAJOR" ]]; then
  echo "ERROR: se requiere Node.js >= ${REQUIRED_NODE_MAJOR}. Actual: $(node -v)" >&2
  exit 1
fi
echo ">> Node: $(node -v)"

if [[ ! -f .env ]]; then
  echo "ERROR: falta .env (copia .env.example y configúralo)" >&2
  exit 1
fi

env_has_value() {
  local key="$1"
  local line
  line="$(grep -E "^${key}=" .env | tail -n1 || true)"
  if [[ -z "$line" ]]; then
    return 1
  fi
  local value="${line#*=}"
  value="${value%\"}"
  value="${value#\"}"
  value="${value%\'}"
  value="${value#\'}"
  [[ -n "$value" ]]
}

for key in TELEGRAM_BOT_TOKEN DATABASE_URL; do
  if ! env_has_value "$key"; then
    echo "ERROR: variable requerida ausente o vacía en .env: $key" >&2
    exit 1
  fi
done
echo ">> .env: variables críticas presentes"

mkdir -p logs

echo ">> Instalando dependencias (npm ci)..."
npm ci

echo ">> Generando cliente Prisma..."
npx prisma generate

echo ">> Compilando TypeScript..."
npm run build

if [[ ! -f dist/index.js ]]; then
  echo "ERROR: no se generó dist/index.js" >&2
  exit 1
fi

echo ">> Podando dependencias de desarrollo..."
npm prune --omit=dev

echo ">> Reiniciando bot con PM2..."
if pm2 describe "$APP_NAME" >/dev/null 2>&1; then
  pm2 restart ecosystem.config.cjs --env production --update-env
else
  pm2 start ecosystem.config.cjs --env production
fi

pm2 save
pm2 status

sleep 2

STATUS="$(pm2 jlist | node -e "
const fs = require('fs');
const apps = JSON.parse(fs.readFileSync(0, 'utf8'));
const app = apps.find((a) => a.name === process.argv[1]);
process.stdout.write(app && app.pm2_env ? (app.pm2_env.status || '') : '');
" "$APP_NAME")"

if [[ "$STATUS" != "online" ]]; then
  echo "ERROR: $APP_NAME no está online (status=${STATUS:-unknown})" >&2
  echo "Revisa logs: pm2 logs $APP_NAME --err --lines 50" >&2
  exit 1
fi

echo "=== Deploy completado ($APP_NAME online) ==="
```

- [ ] **Step 2: Dar permisos de ejecución y validar sintaxis**

Run:

```bash
cd /home/goadmin-bot/htdocs/bot.goadmintravel.com
chmod +x deploy.sh
bash -n deploy.sh && echo "syntax OK"
```

Expected: `syntax OK`

- [ ] **Step 3: Commit (solo si el usuario lo pidió)**

```bash
git add deploy.sh
git commit -m "$(cat <<'EOF'
fix: make deploy.sh build reliably with PM2

EOF
)"
```

---

### Task 3: Alinear `DEPLOY.md` con el flujo real

**Files:**
- Modify: `DEPLOY.md`

**Interfaces:**
- Consumes: comportamiento de Tasks 1–2
- Produces: documentación sin `npm ci --omit=dev` incorrecto; flujo `npm ci` → generate → build → prune

- [ ] **Step 1: Corregir sección 2.4 (install/build)**

En `DEPLOY.md`, reemplazar el bloque de la sección **2.4 Instalar dependencias y compilar** por:

````markdown
### 2.4 Instalar dependencias y compilar

```bash
cd ~/app   # o: cd /home/goadmin-bot/htdocs/bot.goadmintravel.com
npm ci
npx prisma generate
npm run build
npm prune --omit=dev
```

> `typescript` y `prisma` viven en `devDependencies`. Por eso el install de build usa `npm ci` completo y luego `npm prune --omit=dev` deja el runtime limpio.
````

- [ ] **Step 2: Corregir sección 3 Opción B (manual)**

Reemplazar el bloque de **Opción B: Manualmente** por:

````markdown
### Opción B: Manualmente

```bash
git pull origin main
npm ci
npx prisma generate
npm run build
npm prune --omit=dev
pm2 restart travel-telegram-bot --update-env
```
````

- [ ] **Step 3: Aclarar Opción A (script)**

Asegurar que la **Opción A** diga explícitamente que el pull es aparte:

````markdown
### Opción A: Usar el script de deploy

```bash
git pull origin main
bash deploy.sh
```

> `deploy.sh` no hace `git pull`. Valida Node/.env, compila, poda devDeps y reinicia PM2.
````

- [ ] **Step 4: Corregir ruta de logs en estructura (sección 5)**

En la sección **5. Estructura en el servidor**, alinear logs con `./logs` dentro del proyecto (como `ecosystem.config.cjs`), no `~/logs` suelto. Ejemplo:

````markdown
```
/home/goadmin-bot/htdocs/bot.goadmintravel.com/
├── dist/
├── src/
├── prisma/
├── node_modules/
├── ecosystem.config.cjs
├── deploy.sh
├── package.json
├── .env
└── logs/
    ├── error.log
    └── out.log
```
````

(Si el doc aún menciona `~/app` como ruta genérica de Hostinger, dejar una nota de que en este servidor la ruta real es `/home/goadmin-bot/htdocs/bot.goadmintravel.com`.)

- [ ] **Step 5: Verificar que no queden instrucciones `npm ci --omit=dev` incorrectas**

Run:

```bash
cd /home/goadmin-bot/htdocs/bot.goadmintravel.com
rg -n "omit=dev" DEPLOY.md || true
```

Expected: solo menciones de `npm prune --omit=dev` (o ninguna de `npm ci --omit=dev`). Si aparece `npm ci --omit=dev`, eliminarla.

- [ ] **Step 6: Commit (solo si el usuario lo pidió)**

```bash
git add DEPLOY.md
git commit -m "$(cat <<'EOF'
docs: align DEPLOY.md with npm ci + prune flow

EOF
)"
```

---

### Task 4: Ejecutar el deploy y verificar

**Files:**
- None (ejecución)

**Interfaces:**
- Consumes: `deploy.sh` + `ecosystem.config.cjs` + `.env` existentes
- Produces: proceso PM2 `travel-telegram-bot` online

- [ ] **Step 1: Ejecutar el script**

Run:

```bash
cd /home/goadmin-bot/htdocs/bot.goadmintravel.com
bash deploy.sh
```

Expected: termina con `=== Deploy completado (travel-telegram-bot online) ===` y exit code 0.

- [ ] **Step 2: Confirmar estado PM2**

Run:

```bash
pm2 describe travel-telegram-bot | sed -n '1,40p'
pm2 jlist | node -e "const a=JSON.parse(require('fs').readFileSync(0,'utf8')).find(x=>x.name==='travel-telegram-bot'); console.log({status:a.pm2_env.status,node_env:a.pm2_env.NODE_ENV,cwd:a.pm2_env.pm_cwd});"
```

Expected: `status: 'online'`, `node_env: 'production'`, `cwd` = ruta del proyecto.

- [ ] **Step 3: Revisar logs de arranque**

Run:

```bash
pm2 logs travel-telegram-bot --lines 30 --nostream
```

Expected: sin stack traces de fallo al conectar BD/Redis/Telegram; el proceso sigue online.

- [ ] **Step 4: Smoke manual en Telegram**

Enviar un mensaje al bot y confirmar respuesta (verificación humana). Si no hay acceso al bot, documentar que la verificación automática PM2 pasó y queda pendiente el smoke en Telegram.

---

## Spec coverage (self-review)

| Requisito de la spec | Task |
|----------------------|------|
| Validar Node ≥ 22 y `.env` críticas | Task 2 |
| `mkdir logs` | Task 2 |
| `npm ci` → prisma → build → prune | Task 2 |
| PM2 start/restart `--env production` | Task 2 |
| Verificar online + mensaje de logs | Task 2 |
| `cwd: __dirname` en ecosystem | Task 1 |
| Actualizar DEPLOY.md | Task 3 |
| Sin git pull en script | Task 2 (omitido a propósito) |
| Ejecutar y verificar deploy | Task 4 |
| Fuera de alcance Docker/nginx/Laravel/.env secrets | No hay tasks que los toquen |

## Placeholder scan

Sin TBD/TODO. Código completo en Tasks 1–2. Comandos exactos en Tasks 3–4.
